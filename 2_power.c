#include<stdio.h>
#include<math.h>

int main(){
   // printf("the value of 4^5 is %d\n", 4^5); there is no operator to perform exp. in
   printf("the value of 4 to the power 5 is %f\n", pow(4,5));
   // printf("the value of 4^5 is %d\n", 4^5); there is no operator to  perform exp. in c

   return 0;
}